import React from 'react';
import { CreatorProfile } from '../types.ts';

const LoginPage: React.FC<{ onNavigate: (view: any) => void }> = ({ onNavigate }) => {
  return <div>Login Page</div>;
};

export default LoginPage;
