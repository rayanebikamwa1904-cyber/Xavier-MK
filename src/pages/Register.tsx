import React from 'react';
import { CreatorProfile } from '../types.ts';

const RegisterPage: React.FC<{ onNavigate: (view: any) => void }> = ({ onNavigate }) => {
  return <div>Register Page</div>;
};

export default RegisterPage;
