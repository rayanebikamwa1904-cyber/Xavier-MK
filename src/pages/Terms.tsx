import React from 'react';
import { CreatorProfile } from '../types.ts';

const TermsPage: React.FC<{ onNavigate: (view: any) => void }> = ({ onNavigate }) => {
  return <div>Terms Page</div>;
};

export default TermsPage;
