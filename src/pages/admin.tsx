import React from 'react';
import { CreatorProfile } from '../types.ts';

const AdminDashboard: React.FC<{ onNavigate: (view: any) => void }> = ({ onNavigate }) => {
  return <div>Admin Dashboard</div>;
};

export default AdminDashboard;
