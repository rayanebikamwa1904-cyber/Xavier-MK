import React from 'react';
import { CreatorProfile } from '../types.ts';

const PortfolioPreview: React.FC<any> = (props) => {
  return <div>Portfolio Preview</div>;
};

export default PortfolioPreview;
